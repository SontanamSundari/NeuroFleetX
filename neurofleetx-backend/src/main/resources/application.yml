spring:
  application:
    name: neurofleetx-api
  datasource:
    url: jdbc:mysql://localhost:3306/neurofleetx_db
    username: root
    password: sundari@2005
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: false

jwt:
  secret: your-super-secret-jwt-key-change-this-in-production-at-least-32-characters
  expiration: 86400000

server:
  port: 8080
  servlet:
    context-path: /
